#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
  
    long long int a[10],sum=0;
    int n,i;
    scanf("%d",&n);
    for(i=0;i<n;i++)
        {
        scanf("%lli",&a[i]);
            sum=sum+a[i];
    }
    printf("%lli",sum);
    return 0;
}
